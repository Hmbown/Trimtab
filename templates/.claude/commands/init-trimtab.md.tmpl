---
description: Inspect this repository and install or retune its Trimtab workflow so it matches the real project constraints.
argument-hint: [optional notes or scope]
---

Read `.trimtab/init-trimtab-protocol.md` first and follow it exactly.

Treat `$ARGUMENTS` as operator notes, not as permission to skip the repo audit.

Requirements:

- inspect the repo before editing
- preserve stronger existing instructions and conventions
- prefer adapting existing workflow surfaces over creating duplicate ones
- keep `CLAUDE.md`, `AGENTS.md`, `DEPENDENCY_GRAPH.md`, `HANDOFF.md`, and the shared protocol aligned
- keep this Claude command and the repo-stored Codex skill source aligned to the same protocol
- if Linear exists, make it the primary live task surface instead of inventing a second prompt layer
- ask questions only for real blockers or materially ambiguous mappings; otherwise inspect, patch, and continue
- leave a concise durable note of what changed and what still needs tailoring
